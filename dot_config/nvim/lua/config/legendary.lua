require('legendary').setup({
  which_key = {
      -- tell legendary that which-key will be doing the actual key bindings (false = leave it up to which-key)
      do_binding = false,
  },
  -- include_builtin = false
})
